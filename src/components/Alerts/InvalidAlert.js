const InvalidAlert = () => {
    return (
        <div className="alert alert-info" id="invalid-alert" role="alert">
            Invalid guess or pattern
        </div>
    )
}

export default InvalidAlert
